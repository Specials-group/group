<h2>予約履歴</h2>

<% if @orders.present? %>
<table class="orders_index" >
  <tr>
    <th>受取日時</th>
    <th>弁当サイズ</th>
    <th>主食</th>
    <th>主菜</th>
    <th>副菜</th>
    <th>数量</th>
    <th>状態</th>
  </tr>
  <% @orders.zip(@size, @staple, @main, @sub).each do |order,size,staple,main,sub| %>
  <tr>
    <td><%= link_to order.receive_date.strftime("%Y年%m月%d日 %H:%m:"), order %></td>
    <td><%= size %></td>
    <td><%= staple %></td>
    <td><%= main %></td>
    <td><%= sub %></td>
    <td><%= order.num %></td>
    <td><%= order.status %></td>
  </tr>
  <% end %>
</table>
<%= will_paginate @all_order, :previous_label=>'前', :next_label=>'次' %>
<% else %>
  <p>予約履歴がありません</p>
<% end %>
